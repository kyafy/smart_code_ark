# __DISPLAY_NAME__

UniApp + Spring Boot API 模板，适合先做 H5 验证，再扩展到微信小程序或 App。

## 快速开始

1. 启动后端

```bash
cd backend
mvn spring-boot:run
```

1. 启动前端

```bash
cd frontend-mobile
npm install
npm run dev:h5
```

## 默认地址

- API: `http://localhost:8081`
- H5: `http://localhost:5174`
